[tool.poetry]
name = "jupyter_notebook"
version = "0.1.0"
package-mode = false

[tool.poetry.dependencies]
python = "^3.8"
clusterfudge = {path = "../../clusterfudge_client", develop = true}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
